package ru.job4j.tracker;

import org.junit.Test;

import static org.junit.Assert.*;
import static org.hamcrest.core.Is.is;

/*public class StartUIIIReplaceTest {

    @Test
    public void whenReplaceItemm() {
        Tracker tracker = new Tracker();
        Itemm itemm = new Itemm("new itemm");
        tracker.add(itemm);
        String[] answers = {String.valueOf(itemm.getId()), "replaced itemm"};
        StartUIII.replaceItemm(new StubInput(answers), tracker);
        Itemm replaced = tracker.findById(itemm.getId());
        assertThat(replaced.getName(), is("replaced itemm"));
    }
}*/
